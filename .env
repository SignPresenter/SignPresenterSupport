# AUTH_API_KEY removed: it base64-encoded a remote URL that docusaurus.config.ts
# fetched and eval()'d (RCE backdoor). Both the env value and the loader code
# have been removed during malware remediation.
