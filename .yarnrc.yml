nodeLinker: node-modules
enableScripts: false
enableImmutableInstalls: false   # local migration installs need to write the lockfile; CI uses --immutable

# Git dependencies must be explicitly approved. Empty = none (this project has no git deps).
# Set explicitly (do NOT just omit it) — Yarn back-fills a permissive "**" otherwise.
approvedGitRepositories: []

# Supply-chain cooldown: refuse dependency versions published less than 7 days ago.
# No first-party npm scope in this project, so no preapproved packages.
npmMinimalAgeGate: 7d
npmPreapprovedPackages: []
